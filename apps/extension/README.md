# CareerOS Chrome Extension

This Manifest V3 starter captures the active job page and posts a draft to the local Next.js import route.

Load it in Chrome:

1. Open `chrome://extensions`.
2. Enable Developer mode.
3. Select `Load unpacked`.
4. Choose `apps/extension`.

Run the web app on `http://localhost:3000` before saving jobs.
